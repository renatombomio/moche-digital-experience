<section class="contact" aria-label="Contact Moche">

  <div class="contact__inner">

    <header class="contact__header">

      <span class="contact__eyebrow">
        Contact
      </span>

      <div class="contact__brand-row">

        <h2 class="contact__wordmark">
          Moche
        </h2>

        <a
          href="https://www.instagram.com/moche_amsterdam/"
          target="_blank"
          rel="noopener noreferrer"
          class="contact__instagram"
        >
          Instagram
          <span aria-hidden="true">↗</span>
        </a>

      </div>

    </header>

    <div class="contact__information">

      <div class="contact__block">
        <span class="contact__label">Location</span>

        <address class="contact__address">
          <span>Linnaeuskade 3H</span>
          <span>1098 BC Amsterdam</span>
          <span>The Netherlands</span>
        </address>

        <div class="contact__links">
          <a href="tel:+31617273170">+31 6 17273170</a>
          <a href="mailto:hello@moche.nl">hello@moche.nl</a>
        </div>
      </div>

      <div class="contact__block">
        <span class="contact__label">Opening hours</span>

        <div class="contact__hours">
          <div class="contact__hours-row">
            <span>Sunday – Monday</span>
            <strong>Closed</strong>
          </div>

          <div class="contact__hours-row">
            <span>Tuesday – Thursday</span>
            <strong>17:00 – 23:00</strong>
          </div>

          <div class="contact__hours-row">
            <span>Friday – Saturday</span>
            <strong>17:00 – 01:00</strong>
          </div>
        </div>
      </div>

      <div class="contact__block contact__block--notes">
        <span class="contact__label">Good to know</span>

        <div class="contact__notes">
          <div class="contact__note">
            <strong>Dogs are welcome.</strong>
          </div>
          <div class="contact__note">
            <strong>Card payments only.</strong>
          </div>
        </div>
      </div>

    </div>

    <div class="contact__map">

      <iframe
        src="https://www.google.com/maps?q=Linnaeuskade%203H,%201098%20BC%20Amsterdam&output=embed"
        title="Moche Amsterdam location"
        loading="lazy"
        referrerpolicy="no-referrer-when-downgrade"
      ></iframe>

      <a
        href="https://www.google.com/maps/search/?api=1&query=Linnaeuskade%203H%2C%201098%20BC%20Amsterdam"
        target="_blank"
        rel="noopener noreferrer"
        class="contact__map-link"
      >
        Open in Google Maps
        <span aria-hidden="true">↗</span>
      </a>

    </div>

  </div>

</section>

<style>

.contact {
  --contact-black: #050505;
  --contact-cream: #f5f2ea;
  --contact-gold: #c49a4d;
  --contact-muted: rgba(245, 242, 234, 0.62);
  position: relative;
  width: 100%;
  background: var(--contact-black);
  color: var(--contact-cream);
  overflow: hidden;
}

.contact__inner {
  width: min(1180px, calc(100% - 5rem));
  margin: 0 auto;
  padding: 4.5rem 0 3rem;
}

.contact__header {
  margin-bottom: 3.5rem;
  padding-bottom: 1.5rem;
  border-bottom: 1px solid rgba(245,242,234,.2);
}

.contact__eyebrow {
  display: block;
  margin-bottom: 1.2rem;
  color: var(--contact-gold);
  font-family: var(--font-body);
  font-size: .62rem;
  font-weight: 700;
  letter-spacing: .22em;
  text-transform: uppercase;
}

.contact__brand-row {
  display: flex;
  align-items: flex-end;
  justify-content: space-between;
  gap: 2rem;
}

.contact__wordmark {
  margin: 0;
  color: var(--contact-cream);
  font-family: var(--font-display);
  font-size: clamp(4rem,6vw,6rem);
  font-weight: 500;
  line-height: .8;
  letter-spacing: -.07em;
}

.contact__instagram {
  display: inline-flex;
  align-items: center;
  gap: .6rem;
  padding-bottom: .25rem;
  color: var(--contact-cream);
  font-family: var(--font-body);
  font-size: .8rem;
  font-weight: 600;
  letter-spacing: .12em;
  text-transform: uppercase;
  text-decoration: none;
  transition: color .25s ease;
}

.contact__instagram span { color: var(--contact-gold); font-size: 1.1rem; }
.contact__instagram:hover { color: var(--contact-gold); }

.contact__information {
  display: grid;
  grid-template-columns: 1.15fr 1fr .85fr;
  gap: 4rem;
  margin-bottom: 3.5rem;
}

.contact__block { min-width: 0; }

.contact__label {
  display: block;
  margin-bottom: 1.25rem;
  color: var(--contact-gold);
  font-family: var(--font-body);
  font-size: .6rem;
  font-weight: 700;
  letter-spacing: .2em;
  text-transform: uppercase;
}

.contact__address {
  display: flex;
  flex-direction: column;
  gap: .15rem;
  margin: 0;
  color: var(--contact-cream);
  font-family: var(--font-body);
  font-size: 1rem;
  font-style: normal;
  line-height: 1.5;
}

.contact__links {
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  gap: .4rem;
  margin-top: 1.35rem;
}

.contact__links a {
  color: var(--contact-cream);
  font-family: var(--font-body);
  font-size: .95rem;
  font-weight: 500;
  line-height: 1.35;
  text-decoration: none;
  transition: color .25s ease;
}

.contact__links a:hover { color: var(--contact-gold); }

.contact__hours { display: flex; flex-direction: column; gap: 1rem; }
.contact__hours-row { display: flex; flex-direction: column; gap: .25rem; }
.contact__hours-row span { color: var(--contact-muted); font-family: var(--font-body); font-size: .78rem; line-height: 1.35; }
.contact__hours-row strong { color: var(--contact-cream); font-family: var(--font-body); font-size: 1rem; font-weight: 500; line-height: 1.35; }

.contact__notes { display: flex; flex-direction: column; gap: 1rem; }
.contact__note { padding-bottom: 1rem; border-bottom: 1px solid rgba(245,242,234,.18); }
.contact__note:last-child { border-bottom: 0; padding-bottom: 0; }
.contact__note strong { color: var(--contact-cream); font-family: var(--font-body); font-size: 1rem; font-weight: 500; line-height: 1.4; }

.contact__map {
  position: relative;
  width: min(100%, 320px);
  aspect-ratio: 1 / 1;
  overflow: hidden;
  background: #111;
}

.contact__map iframe { display: block; width: 100%; height: 100%; border: 0; }

.contact__map-link {
  position: absolute;
  left: 1rem;
  bottom: 1rem;
  display: inline-flex;
  align-items: center;
  gap: .5rem;
  padding: .65rem .8rem;
  background: rgba(5,5,5,.9);
  color: var(--contact-cream);
  font-family: var(--font-body);
  font-size: .62rem;
  font-weight: 600;
  letter-spacing: .1em;
  text-transform: uppercase;
  text-decoration: none;
  transition: color .25s ease;
}

.contact__map-link span { color: var(--contact-gold); font-size: 1rem; }
.contact__map-link:hover { color: var(--contact-gold); }

@media (max-width: 1000px) and (min-width: 768px) {
  .contact__information { grid-template-columns: 1fr 1fr; gap: 3rem; }
  .contact__block--notes { grid-column: 1 / -1; }
  .contact__map { width: 280px; }
}

@media (max-width: 767px) {
  .contact__inner { width: calc(100% - 2rem); padding: 3.2rem 0 2.2rem; }
  .contact__header { margin-bottom: 2.7rem; padding-bottom: 1.25rem; }
  .contact__eyebrow { margin-bottom: 1rem; font-size: .56rem; }
  .contact__brand-row { align-items: center; gap: 1rem; }
  .contact__wordmark { font-size: clamp(3.5rem,15vw,4.8rem); line-height: .82; }
  .contact__instagram { flex-shrink: 0; font-size: .68rem; letter-spacing: .08em; }
}
</style>